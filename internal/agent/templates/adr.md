---
name: ${NAME}
description: TODO — the decision this record captures, in one line.
status: proposed
---

# ${NAME}

## Status

Proposed. (Managed by `sync-agents adr accept|deny ${NAME}` — the
record's directory under `.agents/adrs/` is the source of truth.)

## Context

What forces are at play; why a decision is needed now.

## Decision

The change we're proposing or have agreed to make.

## Consequences

What becomes easier or harder because of this decision.

## Alternatives considered

Options that were rejected and why. Before proposing a new ADR, check
`.agents/adrs/denied/` — a rejected record there means the idea was
already considered and turned down.
